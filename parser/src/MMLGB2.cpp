#include <bits/types/FILE.h>
#include <iostream>
#include <filesystem>
#include <fstream>
#include <string>
using namespace std;

class MMLGB {
    public:
    static void main(String[] args)
    {
		if(args.length < 2 || args.length > 3) {
			printf("error: Wrong number of arguments.");
			printf("Usage: mmlgb2 MMLFILE OUTFILE [BANK]");

            return 1;
		}

        inpath path current_path();
        String FILE = new string();
    }
}
